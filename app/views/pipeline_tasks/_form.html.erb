<%# The launch composer: describe the work, pick where it runs, say when.
    Locals: task: %>
<%= form_with model: task do |f| %>
  <% if task.errors.any? %>
    <div class="bg-danger/15 text-danger border border-danger/25 px-4 py-3 rounded-lg mb-4 text-sm">
      <% task.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
    </div>
  <% end %>

  <div class="space-y-6" data-controller="task-form">
    <div>
      <%= f.label :title, class: "block text-sm font-medium text-content-muted mb-1.5" %>
      <%= f.text_field :title, placeholder: "NPC dialogue trees",
            class: "w-full px-3 py-2 bg-surface-input border border-edge rounded-lg text-content text-sm focus:outline-none focus:border-accent" %>
    </div>

    <div>
      <%= f.label :project_id, "Project", class: "block text-sm font-medium text-content-muted mb-1.5" %>
      <%= f.select :project_id, Project.order(:name).pluck(:name, :id),
            {}, class: "w-full px-3 py-2 bg-surface-input border border-edge rounded-lg text-content text-sm focus:outline-none focus:border-accent",
            data: { action: "change->task-form#projectChanged", task_form_target: "projectSelect" } %>
    </div>

    <%= render "pipeline_tasks/workflow_cards", f: f, task: task %>

    <div data-controller="code-editor format-body" data-format-body-url-value="<%= format_task_body_path %>">
      <div class="flex items-center justify-between mb-1.5">
        <span class="block text-sm font-medium text-content-muted">Spec</span>
        <button type="button" data-action="format-body#format" data-format-body-target="btn"
                class="inline-flex items-center gap-1.5 px-2.5 py-1 bg-transparent border border-edge text-content-muted rounded-lg text-[0.8125rem] font-medium cursor-pointer hover:bg-surface-input hover:text-content transition-colors">
          Format with Claude
        </button>
      </div>
      <div class="codejar-wrap">
        <code class="language-markdown codejar-editor" data-code-editor-target="editor"></code>
      </div>
      <%= f.hidden_field :body, data: { code_editor_target: "input", format_body_target: "input" } %>
    </div>

    <%= render "pipeline_tasks/relevant_files", f: f, task: task %>

    <%= render "trigger_fields", f: f, task: task %>

    <details>
      <summary class="text-sm text-content-muted cursor-pointer select-none hover:text-content">Advanced</summary>
      <div class="mt-3">
        <%= f.label :kind, "Tag", class: "block text-sm font-medium text-content-muted mb-1.5" %>
        <%= f.select :kind, PipelineTask::KINDS.map { |k| [k.capitalize, k] },
              {}, class: "w-full md:w-64 px-3 py-2 bg-surface-input border border-edge rounded-lg text-content text-sm focus:outline-none focus:border-accent" %>
        <p class="text-xs text-content-muted mt-1.5">
          Available to steps as <code class="bg-surface-input px-1 rounded">${task_kind}</code>. Defaults to feature.
        </p>
      </div>
    </details>

    <div class="flex gap-2 pt-2 border-t border-edge">
      <%= button_tag "Launch", type: "submit", name: "run_now", value: "1",
            class: "inline-flex items-center gap-1.5 px-4 py-2 bg-accent text-white rounded-lg text-sm font-medium cursor-pointer hover:bg-accent-hover transition-colors" %>
      <%= f.submit "Save draft", class: "inline-flex items-center gap-1.5 px-4 py-2 bg-transparent border border-edge text-content-muted rounded-lg text-sm font-medium cursor-pointer hover:bg-surface-input hover:text-content transition-colors" %>
      <%= link_to "Cancel", (task.persisted? ? pipeline_task_path(task) : pipeline_tasks_path),
            class: "inline-flex items-center gap-1.5 px-4 py-2 bg-transparent border border-edge text-content-muted rounded-lg text-sm font-medium hover:bg-surface-input hover:text-content transition-colors" %>
    </div>
  </div>
<% end %>
