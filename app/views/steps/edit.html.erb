<%= render "shared/project_bar", project: @project, workflow: @workflow %>
<h1 class="text-2xl font-semibold mb-4">Edit Step: <%= @step.name %></h1>
<div class="bg-surface-card border border-edge rounded-lg p-5">
  <%= render "form", step: @step %>
</div>
