<%# Shell step: runs in the run's checkout. Locals: f:, step: %>
<div>
  <%= f.label :body, "Command", class: "block text-sm font-medium text-content-muted mb-1.5" %>
  <%= f.text_area :body, rows: 8,
        placeholder: "e.g. npm run build",
        class: "w-full px-3 py-2 bg-surface-input border border-edge rounded-lg text-content text-sm font-mono focus:outline-none focus:border-accent resize-y" %>
  <p class="text-xs text-content-muted mt-1.5">
    Runs in the working copy for this run. Variables like <code class="bg-surface-input px-1 rounded">${task_title}</code>
    are substituted first. Write outputs with <code class="bg-surface-input px-1 rounded">::set-output</code>.
  </p>
</div>
