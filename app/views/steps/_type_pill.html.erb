<%# Delegates color duty to BadgeHelper#type_badge so step types read the same
    everywhere; the wrapper only preserves this pill's flex behaviour. %>
<span class="shrink-0 ml-2"><%= type_badge(step_type) %></span>
