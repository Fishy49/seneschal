<%= render layout: "shared/components/card", locals: { title: "Security", class: "mb-4" } do %>
  <div class="flex items-start justify-between gap-3">
    <div class="min-w-0">
      <div class="text-sm font-medium">
        Two-factor authentication
        <% if current_user.otp_required_for_login? %>
          <span class="ml-2 text-xs text-success">enabled</span>
        <% else %>
          <span class="ml-2 text-xs text-content-muted">not set</span>
        <% end %>
      </div>
      <p class="text-xs text-content-muted mt-1">
        <% if current_user.otp_required_for_login? %>
          Signing in asks for a code from your authenticator app.
        <% else %>
          Add a code from an authenticator app on top of your password.
        <% end %>
      </p>
    </div>
    <% if current_user.otp_required_for_login? %>
      <%= button_to "Disable", disable_two_factor_path, method: :post,
            class: "shrink-0 inline-flex items-center gap-1.5 px-2.5 py-1 bg-transparent border border-edge text-content-muted rounded-lg text-[0.8125rem] font-medium cursor-pointer hover:text-danger transition-colors",
            data: { turbo_confirm: "Disable two-factor authentication?" } %>
    <% else %>
      <%= link_to "Enable two-factor authentication", setup_two_factor_path,
            class: "shrink-0 inline-flex items-center gap-1.5 px-2.5 py-1 bg-transparent border border-edge text-content-muted rounded-lg text-[0.8125rem] font-medium hover:bg-surface-input hover:text-content transition-colors" %>
    <% end %>
  </div>
<% end %>
