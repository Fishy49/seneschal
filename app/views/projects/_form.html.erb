<%= form_with model: project do |f| %>
  <% if project.errors.any? %>
    <div class="bg-danger/15 text-danger border border-danger/25 px-4 py-3 rounded-lg mb-4 text-sm">
      <% project.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
    </div>
  <% end %>

  <div class="mb-4">
    <%= f.label :name, class: label_classes %>
    <%= f.text_field :name, placeholder: "supertextadventure", class: input_classes %>
  </div>

  <div class="mb-4">
    <%= f.label :repo_url, "Repository URL", class: label_classes %>
    <%= f.text_field :repo_url, placeholder: "git@github.com:user/repo.git", class: input_classes %>
  </div>

  <div class="mb-4" data-controller="default-path" data-default-path-base-value="<%= Rails.root.join('repos') %>">
    <%= f.label :local_path, "Local Path", class: label_classes %>
    <div class="flex gap-2">
      <%= f.text_field :local_path, placeholder: "#{Rails.root.join('repos', 'myproject')}",
            data: { default_path_target: "input" },
            class: input_classes("flex-1") %>
      <button type="button"
              class="<%= btn_classes(:ghost, size: :sm) %>"
              data-default-path-target="btn"
              data-action="default-path#populate">
        Default
      </button>
    </div>
    <p class="<%= hint_classes %>">Leave free-form or click Default to use repos/ in the app root.</p>
  </div>

  <div class="mb-4">
    <%= f.label :description, class: label_classes %>
    <%= f.text_area :description, placeholder: "Optional description",
          class: input_classes("resize-y min-h-20") %>
  </div>

  <div class="mb-4" data-controller="inline-group-create">
    <%= f.label :project_group_id, "Project Group", class: label_classes %>
    <div class="flex gap-2">
      <%= f.select :project_group_id,
            options_for_select(ProjectGroup.ordered.pluck(:name, :id), project.project_group_id),
            { include_blank: "(no group)" },
            data: { inline_group_create_target: "select" },
            class: input_classes("flex-1") %>
      <button type="button"
              class="<%= btn_classes(:ghost, size: :sm, extra: "whitespace-nowrap") %>"
              data-inline-group-create-target="toggle"
              data-action="inline-group-create#open">
        + New
      </button>
    </div>
    <div class="hidden mt-2" data-inline-group-create-target="form">
      <div class="flex gap-2">
        <input type="text"
               placeholder="New group name"
               data-inline-group-create-target="input"
               data-action="keydown->inline-group-create#keydown"
               class="<%= input_classes("flex-1") %>">
        <button type="button"
                class="<%= btn_classes(:primary, extra: "whitespace-nowrap disabled:opacity-50 disabled:cursor-not-allowed") %>"
                data-inline-group-create-target="submit"
                data-action="inline-group-create#submit">
          Create
        </button>
        <button type="button"
                class="<%= btn_classes(:ghost, extra: "whitespace-nowrap") %>"
                data-action="inline-group-create#cancel">
          Cancel
        </button>
      </div>
      <p class="hidden mt-1.5 text-sm text-danger" data-inline-group-create-target="error"></p>
    </div>
  </div>

  <div class="mb-4" data-controller="code-editor">
    <%= f.label :markdown_context, "Project Context (Markdown)", class: label_classes %>
    <p class="<%= hint_classes("mb-2") %>">Shared guidance auto-injected into Claude Code CLI invocations for every workflow step under this project. Optional.</p>
    <div class="codejar-wrap">
      <code class="language-markdown codejar-editor" data-code-editor-target="editor"></code>
    </div>
    <%= f.hidden_field :markdown_context, data: { code_editor_target: "input" } %>
  </div>

  <div class="mb-6 p-4 bg-danger/5 border border-danger/30 rounded-lg">
    <div class="flex items-start gap-3">
      <div class="flex-1">
        <label class="block text-sm font-semibold text-danger mb-1">Danger Mode</label>
        <p class="text-sm text-content-muted mb-3">When enabled, Claude will run with <code class="font-mono text-xs bg-surface-input px-1 py-0.5 rounded">--dangerously-skip-permissions</code> and may take destructive actions without confirmation. Use only in trusted, isolated environments.</p>
        <label class="flex items-center gap-2 cursor-pointer">
          <%= f.check_box :skip_permissions, class: "w-4 h-4 accent-danger" %>
          <span class="text-sm text-content">Enable danger mode for this project</span>
        </label>
      </div>
    </div>
  </div>

  <div class="flex gap-2">
    <%= f.submit class: btn_classes(:primary) %>
    <%= link_to "Cancel", projects_path, class: btn_classes(:ghost) %>
  </div>
<% end %>
